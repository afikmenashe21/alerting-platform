# SMTP Email Configuration
# Copy this file to .env and fill in your actual credentials
# Never commit .env to version control

# SMTP Server Configuration
SMTP_HOST=localhost
SMTP_PORT=1025

# SMTP Authentication (optional for local MailHog, required for Gmail)
SMTP_USER=
SMTP_PASSWORD=

# Email From Address
SMTP_FROM=alerts@alerting-platform.local

# Gmail Configuration Example (uncomment and fill in):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=your-email@gmail.com

# Logging
# LOG_LEVEL=INFO
